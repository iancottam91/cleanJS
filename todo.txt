Command prompt:

- add all steps in journey
- add parameter for custom fn - can replace addContent:
* should explain what parameters it expects and what it should return (in docs and cmd prompt)
- add error handling
- test